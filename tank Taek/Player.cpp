#include "Player.h"

// กำหนดค่าเริ่มต้นให้ Player: พลังชีวิต=3, พลังกระสุน=1, สัญลักษณ์ 'P'
// และตั้งทิศทางเริ่มต้นให้หันขึ้น (0,-1)
Player::Player(int startX, int startY)
    : Tank(startX, startY, 3, 1, 'P'), ทิศทางX(0), ทิศทางY(-1) {
    // ผู้เล่นเริ่มต้นหันขึ้นบน (ทิศทางY = -1)
}
